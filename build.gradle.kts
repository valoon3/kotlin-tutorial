plugins {
    kotlin("jvm") version "2.1.10"
}

// Root aggregator build. Configure repositories for all modules.
allprojects {
    repositories {
        mavenCentral()
    }

    dependencies {
        
    }
}
